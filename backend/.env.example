# Optional Neo4j — leave NEO4J_URI unset to use sample JSON only (local dev without graph).
# When set, /relics loads up to 10 nodes labeled :Relic; on failure or empty → JSON fallback.
NEO4J_URI=
NEO4J_USER=neo4j
NEO4J_PASSWORD=password
